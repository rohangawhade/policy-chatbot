# Staging environment template (files/plan.md Step 14.6). Copy to
# .env.staging and fill in real values — never commit .env.staging.
#
# This mirrors .env.example's full variable list exactly (see that file
# for what every variable does) with only the values that should
# realistically differ for a staging deployment changed below:
# APP_ENV/APP_DEBUG (drives Step 14.1's JSON-vs-console structlog
# output — staging wants JSON, same as production, to validate the log
# pipeline before it matters for real), and the CORS/frontend origins
# (a staging deployment isn't served from localhost). Used together
# with docker-compose.staging.yml:
#   docker compose -f docker-compose.yml -f docker-compose.staging.yml up -d

# --- App ---
APP_ENV=staging
APP_DEBUG=false
# CHANGE ME: a real, unique secret for this environment -- never reuse
# the "changeme" placeholder or another environment's value.
APP_SECRET_KEY=changeme
APP_UPLOAD_DIR=./uploads
APP_MAX_UPLOAD_SIZE_MB=25

# --- Database (SQLAlchemy async + Alembic) ---
DATABASE_URL=postgresql+asyncpg://policypal:policypal@localhost:5432/policypal
DATABASE_POOL_SIZE=10

# --- Redis (cache + Celery broker/backend) ---
REDIS_URL=redis://localhost:6379/0

# --- Cache ---
CACHE_TTL_SECONDS=3600

# --- Celery ---
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/2

# --- Pinecone ---
PINECONE_API_KEY=
PINECONE_ENVIRONMENT=
PINECONE_INDEX_NAME=policypal-staging

# --- LLM / LiteLLM — model tiers are config-driven ---
# If LLM_POWERFUL_MODEL is empty or its provider key is missing, all queries
# fall back to LLM_CHEAP_MODEL automatically. No code changes needed.
LLM_CHEAP_MODEL=claude-haiku-4-5-20251001
LLM_POWERFUL_MODEL=claude-sonnet-4-6
LLM_EMBEDDING_MODEL=llama-text-embed-v2
LLM_COMPLEXITY_THRESHOLD=0.4
LLM_FALLBACK_ENABLED=true
LLM_TEMPERATURE=0.1
LLM_MAX_TOKENS=2048
LLM_STREAMING_ENABLED=true
LLM_DAILY_COST_ALERT_THRESHOLD_USD=50.0

# Provider keys used by LiteLLM, depending on which models you configure above.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# --- Retry policy (exponential backoff with jitter, external calls) ---
RETRY_LLM_GENERATION_MAX_ATTEMPTS=3
RETRY_LLM_EMBEDDING_MAX_ATTEMPTS=2
RETRY_PINECONE_MAX_ATTEMPTS=3
RETRY_REDIS_MAX_ATTEMPTS=3
RETRY_BASE_DELAY_SECONDS=1.0
RETRY_MAX_DELAY_SECONDS=10.0

# --- Rate limiting (Redis-backed sliding window, LLM-calling endpoints) ---
RATE_LIMIT_CHAT_MAX_REQUESTS=20
RATE_LIMIT_CHAT_WINDOW_SECONDS=60

# --- Auth (OAuth2 + JWT) ---
# CHANGE ME: a real, unique secret for this environment.
AUTH_JWT_SECRET_KEY=changeme
AUTH_JWT_ALGORITHM=HS256
AUTH_ACCESS_TOKEN_EXPIRE_MINUTES=15
AUTH_REFRESH_TOKEN_EXPIRE_DAYS=7

# --- CORS ---
# CHANGE ME: the real staging frontend origin.
CORS_ALLOWED_ORIGINS=https://staging.policypal.example

# --- Frontend (Vite exposes only VITE_-prefixed vars to the client) ---
# CHANGE ME: the real staging backend origin.
VITE_API_BASE_URL=https://staging-api.policypal.example
