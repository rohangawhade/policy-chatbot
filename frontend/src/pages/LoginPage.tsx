// Placeholder -- the real login form + role-based redirect lands in
// Step 10.2 (files/plan.md). This step only needs a real route target.
export default function LoginPage() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <h1 className="text-2xl font-medium">Log in to PolicyPal</h1>
    </div>
  );
}
