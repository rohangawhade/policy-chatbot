// Placeholder -- the real employer portal (self-serve upload, employee
// management, policy overview) lands in Step 10.8 (files/plan.md). This
// step only needs a real route target.
export default function EmployerPortal() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <h1 className="text-2xl font-medium">Employer portal</h1>
    </div>
  );
}
