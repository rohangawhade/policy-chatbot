// Placeholder -- the real admin dashboard (document/employer management,
// analytics, quality monitoring, operational health) lands across Steps
// 10.4-10.7 (files/plan.md). This step only needs a real route target.
export default function AdminDashboard() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <h1 className="text-2xl font-medium">Admin dashboard</h1>
    </div>
  );
}
